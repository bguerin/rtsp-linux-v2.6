language: c

before_install:
  - sudo apt-get update
  - wget https://ftp-master.debian.org/keys/archive-key-7.0.asc

install:
  - sudo apt-get install pbuilder gnupg
  - gpg --import $PWD/archive-key-7.0.asc

before_script:
  - sudo pbuilder create --distribution wheezy --mirror http://http.debian.net/debian/ --debootstrapopts --keyring=$HOME/.gnupg/pubring.gpg
  - mkdir ~/pbuilderhooks
  - cp /usr/share/doc/pbuilder/examples/B90lintian $HOME/pbuilderhooks
  - echo "HOOKDIR=$HOME/pbuilderhooks/" >> ~/.pbuilderrc

script:
  - pdebuild
